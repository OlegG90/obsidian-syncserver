.git
node_modules
**/node_modules
**/dist
# Same for shared/, which emits declarations into types/ rather than dist/. The build stage
# compiles them from source; a stale local copy has no business in the context.
shared/types
data
var
docs
plugin/src
# console/src is NOT excluded: the image builds it and the server serves it.
**/*.test.ts
.env
.env.*
**/*.tsbuildinfo
