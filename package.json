{
  "name": "syncserver-workspace",
  "version": "0.7.6",
  "private": true,
  "description": "Self-hosted synchronisation server for Obsidian vaults: multi-device sync plus per-folder sharing, end-to-end encrypted",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "shared",
    "server",
    "console",
    "plugin"
  ],
  "scripts": {
    "db:reset": "./tools/db-reset.sh",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run check:version && npm run check:citations && npm run check:names && npm run check:registration && npm run check:config-scope && npm run test --workspaces --if-present",
    "check:compose": "node checks/check-compose.mjs",
    "check:version": "node checks/check-version.mjs",
    "check:docblocks": "node checks/check-docblocks.mjs",
    "check:citations": "node checks/check-citations.mjs",
    "check:names": "node checks/check-names.mjs",
    "check:registration": "node checks/check-registration.mjs",
    "check:config-scope": "node checks/check-config-scope.mjs",
    "pack": "./tools/pack.sh",
    "test:live": "SYNCSERVER_DB=syncserver_plugin ./tools/db-reset.sh && npm run build --workspace @syncserver/server && npm run test --workspace @syncserver/plugin",
    "clean": "./tools/clean.sh"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "yaml": "^2.9.0"
  }
}
