{
  "name": "@syncserver/console",
  "version": "0.7.8",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node esbuild.mjs",
    "typecheck": "tsc -b ../shared && tsc --noEmit && tsc --noEmit -p tsconfig.test.json",
    "test": "tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@syncserver/shared": "*"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "tsx": "^4.23.9"
  }
}
